import { useState } from 'react';

const ITEMS = [
  {
    q: 'Что такое The Billion Milestone?',
    a: 'Это рейтинг участников, которые приглашают друзей. Чем больше активных друзей в вашей сети — тем выше ваш счёт и место в таблице.',
  },
  {
    q: 'Как считается мой счёт?',
    a: 'Счёт = 1000 × ln(1 + Личные + 0.5 × Второй уровень + 0.25 × Третий уровень). Учитываются приглашённые вами, а также те, кого пригласили они.',
  },
  {
    q: 'Сколько уровней учитывается?',
    a: 'Три уровня. Первый — те, кого вы пригласили лично. Второй — те, кого пригласили они. Третий — на уровень глубже. Дальше не учитывается.',
  },
  {
    q: 'Как пригласить друга?',
    a: 'Откройте модуль INVITE — там ваша персональная ссылка. Отправьте её другу, и он автоматически попадёт в вашу сеть после регистрации.',
  },
  {
    q: 'Что делать, если мой счёт не обновляется?',
    a: 'Счёт пересчитывается автоматически при регистрации новых участников. Если что-то кажется неправильным — обновите страницу или подождите минуту.',
  },
  {
    q: 'Куда пропадают мои очки?',
    a: 'Очки не пропадают. Если участник удалён (например, заблокировал бота), его вклад пересчитывается, и счёт может уменьшиться.',
  },
];

export default function Faq() {
  const [open, setOpen] = useState(null);

  return (
    <div className="faq">
      {ITEMS.map((item, i) => (
        <div
          key={i}
          className={`faq-item ${open === i ? 'faq-item-open' : ''}`}
        >
          <button
            className="faq-question"
            onClick={() => setOpen(open === i ? null : i)}
          >
            <span>{item.q}</span>
            <span className="faq-toggle">{open === i ? '−' : '+'}</span>
          </button>
          {open === i && (
            <div className="faq-answer">
              <p>{item.a}</p>
            </div>
          )}
        </div>
      ))}
    </div>
  );
}
